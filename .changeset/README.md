# Changesets

Run `pnpm changeset` to declare a release. Each file here describes the version
bump and the user-facing note. The "Version Packages" PR opened by the release
workflow rolls them into the changelog and bumps versions.
